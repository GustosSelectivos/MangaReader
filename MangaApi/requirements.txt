# ─── FastAPI Core ────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12       # Necesario para UploadFile / Form

# ─── Base de Datos (MySQL async) ─────────────────────────────────────────────
sqlalchemy==2.0.36
aiomysql==0.2.0                # Driver MySQL async pure-Python (sin compilador C++)
greenlet>=3.0.0                # Requerido por SQLAlchemy async en Windows
alembic==1.14.0                # Migraciones de esquema

# ─── Validación / Serialización ──────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1       # Reemplaza settings.py de Django

# ─── Autenticación JWT ───────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ─── Infraestructura B2 (Backblaze async) ────────────────────────────────────
aiobotocore==2.15.2            # Reemplaza boto3 síncrono
aiofiles==24.1.0               # Lectura/escritura de archivos async

# ─── Caché / Redis ───────────────────────────────────────────────────────────
redis[asyncio]==5.2.1          # Counter middleware (con fallback en memoria)

# ─── HTTP Client (Worker de capítulos) ───────────────────────────────────────
httpx==0.28.1                  # Reemplaza requests síncrono en chapter_view

# ─── Observabilidad ──────────────────────────────────────────────────────────
sentry-sdk[fastapi]==2.19.2

# ─── Utilidades ──────────────────────────────────────────────────────────────
python-dotenv==1.0.1
python-slugify==8.0.4          # Generación de slugs (reemplaza django.utils.text.slugify)

cachetools==5.3.3

# ─── Seguridad (Rate Limiting) ───────────────────────────────────────────────
slowapi==0.1.9
